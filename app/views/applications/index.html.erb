
  <p style="color: green"><%= notice %></p>

  <h1 class="apps">Applications</h1> <br>

  <%= form_tag(search_path, :method => "get") do %>
    <div class="container">
      <%= search_field_tag :search, params[:search], placeholder: "Search for title ", class: "form-control" %>
      <div class="input-group-button">
        <%= button_tag "Search", :class => 'button', :title => nil %>
      </div>
    </div>
  <% end %>

  <div id="applications">
    <% @applications.each_slice(3) do |applications| %>
          <%- applications.each do |application| %>
            <%= render application%>
          <%end%>
    <% end %>
  </div>
